
## 一、总则

1. 基准体系：**8px 栅格系统**，所有间距、尺寸优先使用 `4/8/12/16/24/32/48/64px`；禁止随意自定义 5px、7px、13px 等不规则数值。
2. 统一原则：全局圆角、边框粗细、阴影、交互反馈保持一致；所有样式统一维护全局 CSS 变量，禁止页面硬编码色值、尺寸。
3. 像素要求：元素宽高、坐标使用整数 px，禁止出现 0.5px（容易模糊）。

## 二、整体页面布局规范

### 2.1 页面结构划分

页面统一结构：`侧边导航区 + Page内容容器`

1. **内容容器（.page-container）**

```
max-width: 1440px; /* 大屏上限 */
margin: 0 auto;
padding: 24px; /* 页面四周安全边距，内容不能紧贴浏览器边框 */
```

2. 模块层级：页面 → 卡片模块 → 模块标题区 → 内容区 → 控件组
3. 布局推荐 **12 列栅格**（后台系统通用）

- 水槽 (gutter)：24px（列与列横向间距）
- 卡片、表单、查询区域对齐栅格竖线；通栏区块可突破栅格。

### 2.2 模块通用（Card 卡片）

```
padding: 24px; /* 卡片内部基础内边距 */
border-radius: 6px;
border: 1px solid #e5e7eb;
```

- 卡片与卡片垂直间距：**24px**
- 卡片内「标题」与下方内容垂直间距：**16px**

## 三、间距规范（核心！所有 margin/padding 严格遵守）

采用 8 增量刻度，分为 6 类场景

表格

| 数值 | 命名 | 使用场景 |
| --- | --- | --- |
| 4px | xs | 图标与文字最小间隙、标签内部微调（极少用） |
| 8px | sm | 表单标签与输入框、同行多个按钮间距、列表内元素间距 |
| 12px | md | 按钮上下内边距、小标题与内容间距 |
| 16px | base | **最常用**；组件内基础 padding、段落间距 |
| 24px | lg | 卡片内边距、模块内分组间距 |
| 32px | xl | 模块与模块、不同卡片之间垂直距离 |
| 48px | xxl | 页面大分区、页面顶部大块留白 |

> 
> ❌ 禁止：相邻两个元素同时设置上下 margin 造成间距叠加；统一单边 margin（优先使用`margin-bottom`控制垂直距离）

## 四、文字 / 排版规范（Typography）

### 4.1 全局字体

```
font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
```

### 4.2 文字层级（PC 后台标准）

表格

| 层级 | 字号 | 字重 | 行高 | 用途 |
| --- | --- | --- | --- | --- |
| Page 标题 | 24px | 600 | 1.3 | 页面大标题 |
| 模块标题 | 20px | 600 | 1.4 | Card 头部标题 |
| 小标题 | 16px | 500 | 1.5 | 分组小标题 |
| 正文 | 14px | 400 | 1.6 | **绝大多数页面文字（表单、表格、内容）** |
| 辅助文字 | 13px | 400 | 1.6 | 备注、提示、表格次要信息 |
| 说明小字 | 12px | 400 | 1.6 | 底部注释、输入框提示语 |

### 4.3 文字颜色规范（中性色系）

- 主要文本：`#1f2937`（标题、正文）
- 次要文本：`#4b5563`
- 辅助 / 提示文字：`#9ca3af`
- 链接文字：主色，hover 加深色值

### 4.4 排版规则

1. 段落文字行高最低 1.5，避免文字拥挤；
2. 表单 label 文字统一右对齐 / 左对齐，项目内保持一套；
3. 长文本禁止单行无限拉长，合理利用宽度限制；
4. 表格内文字垂直居中。

## 五、按钮组件规范

### 5.1 尺寸规格（四种尺寸）

1. **大型按钮 (L)**
高度：40px；padding：`0 24px`；字号 14px；圆角 6px
2. **中型按钮 (M，默认)**
高度：36px；padding：`0 20px`；字号 14px；圆角 6px
3. **小型按钮 (S)**
高度：32px；padding：`0 16px`；字号 13px；圆角 4px
4. **迷你按钮 (XS，表格内操作)**
高度：28px；padding：`0 12px`；字号 12px；圆角 4px

### 5.2 按钮类型

1. **Primary 主按钮**（主要行动：提交、保存、确认）
实心背景 + 白色文字
2. **Secondary 次按钮**（取消、返回）
边框 + 透明底色
3. **Text 文字按钮**（表格编辑、查看）
无背景无边框，仅文字可点击
4. **Danger 危险按钮**（删除、销毁操作）

### 5.3 交互状态（必须完整实现）

`默认态 → hover → active点击 → disabled禁用 → loading加载`

- 禁用按钮：opacity:0.55，禁止鼠标点击事件
- 图标 + 文字按钮：图标与文字间距固定 **8px**

### 5.4 布局使用规则

1. 表单底部操作按钮，主按钮放左侧；
2. 多个按钮横向排布，按钮之间间距 **8px**；
3. 弹窗底部操作区与上方内容间距 24px。

## 六、分割线规范（Divider）

### 6.1 线条基础属性

- 线条粗细：**1px**（统一，不要混用 2px）
- 颜色：`#e5e7eb`
- 圆角：0px（直线分割）

### 6.2 两种分割场景

1. **模块横向分割线（水平）**

- 完整通栏分割：左右无缩进
- 内容内部分割：左右缩进 24px，距离上下内容 `16px`

2. **垂直分割线（按钮组、标签之间）**
高度自适应，margin 左右 12px；**不要单独使用空 div 实现分割，优先组件自带分割线**

### 6.3 使用禁忌

1. 不要用分割线代替 margin 间距！模块之间优先留白，分割线只用于强区分场景；
2. 禁止多处同时使用分割线造成界面杂乱。

## 七、通用基础样式约束（开发强制遵守）

1. **圆角统一值**

- 卡片、弹窗：6px
- 输入框、中型按钮：6px
- 小按钮、标签：4px

> 
> 项目内仅允许使用 4px / 6px 两套圆角，禁止出现 8px、10px 随意混用。

2. 边框统一：所有常规边框 `1px solid #e5e7eb`
3. 禁止直接写死 px 在页面；推荐使用 CSS 变量统一管理示例：

```
:root {
  --space-8: 8px;
  --space-16: 16px;
  --radius-md: 6px;
  --text-base: 14px;
}
```

## 八、页面布局开发通用 Checklist（评审自查清单）

1. ✅ 页面四周是否保留 24px 安全边距，内容没有贴边
2. ✅ 所有 margin/padding 数值都是 4/8 倍数，无自定义小数、奇数间距
3. ✅ 模块垂直间距统一使用 margin-bottom，没有 margin 上下叠加
4. ✅ 文字字号、颜色严格遵循层级，不存在随意 15px、17px 字号
5. ✅ 按钮尺寸、圆角统一，多个按钮之间间距 8px
6. ✅ 分割线上下留白一致，不紧贴文字
7. ✅ 卡片内 padding 统一 24px，卡片间距 24px
8. ✅ 全部元素坐标宽高整数 px，无 0.5px 模糊问题