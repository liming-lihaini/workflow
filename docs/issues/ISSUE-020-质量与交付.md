# ISSUE-020：质量与交付 — 测试 / 压测 / Docker / 文档

- **优先级**：-
- **模块**：质量与交付
- **负责人**：全员（测试主导）
- **预计工期**：W13-W14
- **前置依赖**：ISSUE-001 ~ ISSUE-019（全部）
- **里程碑**：M5

## 目标
完成全量联调、单元/集成测试、性能压测（对齐 PRD 非功能基线）、Docker Compose 一键部署与交付文档，达成上线门禁（M5）。

## 范围
### In Scope
- 测试：各 Issue 单元/集成测试汇总；端到端跑通“请假流程”全链路（设计→发起→会签/加签→完成→监控）。
- 性能压测：单实例创建 < 500ms、节点执行 < 200ms、并发实例 > 1000、模板 > 10000（PRD §性能需求）。
- 部署：Docker Compose（TRD §7.2，SQLite 挂载卷）；镜像 `flow-engine:latest`。
- 文档：用户手册、部署手册、API 文档（汇总 TRD §3）、本计划与 Issue 清单归档。
- 非功能核对：可用性 >99.9%/集群（TRD §7.1）、安全（JWT/脱敏/审计，§6）。

### Out of Scope
- 新功能开发（此时仅修复与调优）。

## 技术约束（以 TRD 为准） 
- Docker Compose 见 TRD §7.2（`jdbc:sqlite:/data/flow_engine.db` + 卷）。
- 性能基线见 PRD §性能需求；错误码/响应见 TRD §8。

## 接口契约
- 无新增接口；对既有 `/api/v1/*` 做全量回归。

## 独立运行与验证
### 运行方式
```bash
mvn test                                   # 全量单元/集成测试
# 压测脚本（JMeter/k6）对 /instances 发起并发
docker compose up -d                       # 部署并验证健康检查
```

### 验证用例（映射 PRD §非功能性需求 / TRD §7/§8）
- [ ] 全量测试通过（`mvn test` 0 失败）；端到端请假流程跑通。
- [ ] 单实例创建 p95 < 500ms；节点执行 p95 < 200ms。
- [ ] 并发 > 1000 实例无丢失；模板库 > 10000 可正常查询。
- [ ] `docker compose up` 后 `/actuator/health` UP；数据持久化于挂载卷。
- [ ] 交付文档齐备（用户/部署/API 手册）。
- [ ] 安全：JWT 鉴权、敏感脱敏、操作审计均生效（对应 013/014/016）。

### 验证脚本/测试
- `mvn test` 汇总报告；k6/JMeter 压测脚本与报告；Docker 部署冒烟脚本。

## 交付物
- 测试报告、压测报告、Docker Compose、用户/部署/API 文档。

## 验收门禁（DoD）
- 全量测试通过；性能达 PRD 基线；Docker 一键部署可用；文档齐备。
