- 路由采用 `component: () => import('../views/...')` 的函数式动态导入，按页面粒度进行代码分割。
- 每个业务域在 `src/api/` 下独立文件暴露方法，统一通过 `import request from './request'` 发起 HTTP 请求，不在视图层直接调用 axios。
- 路由元信息通过 `meta.title` 描述页面标题，供 BasicLayout 渲染侧边栏菜单。
- 鉴权状态以 `localStorage.getItem('token')` 读取，未登录时路由守卫与 401 响应拦截器共同重定向至 `/login`。